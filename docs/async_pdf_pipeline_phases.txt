async-pdf-pipeline`nBuild Phases Plan`n4 phases  |  21 days  |  high-level goals  |  one file to rule them all`n`nPhase`nName`nDays`nWhat you have at the end`n1`nFoundation`n1–4`nPDF uploads, chunks, job_id, Redis tracking — no LLM`n2`nBeam`n5–9`nChunks fan out in parallel on real Beam containers`n3`nLLM output`n10–13`nFull structured JSON from every upload, MD5 cache works`n4`nPolish & deploy`n14–21`nLive URL, Streamlit demo, benchmark numbers, README`n`n`nPhase 1  —  Foundation`nDays 1–4`n`nGoal`nBy end of day 4 you can upload a PDF with curl, it gets OCR'd by Docling and split into chunks, a job_id comes back immediately, and you can poll /status to see done_chunks. Redis tracks every job. No Beam, no LLM — just clean plumbing that works end-to-end.`n`nDay 1 — project scaffold`nCreate the repo: async-pdf-pipeline`nSet up virtual environment and install requirements.txt`nCopy .env.example to .env — fill in REDIS_URL (local: redis://localhost:6379)`nStart local Redis: docker run -d -p 6379:6379 redis:7-alpine`nCreate the folder structure: api/, beam/, frontend/, tests/`nWrite FastAPI skeleton in main.py — POST /ingest returns hardcoded job_id for now`nConfirm: uvicorn api.main:app --reload starts without errors`n`nDay 2 — tracker.py (all Redis helpers)`nWrite the complete tracker.py — all three groups of helpers in one file from day one`nJob group: init_job(), push_result(), get_job(), set_final_output()`nRate limit group: check_rate_limit(), record_upload(), get_rate_limit_status()`nMD5 group: calculate_file_md5(), get_cached_result(), cache_result_by_md5()`nTest each function manually in a Python shell — verify keys appear in Redis`nConfirm: RPUSH + HINCRBY pipeline is atomic, no race conditions`n`nDay 3 — chunker.py (Docling + splitter)`nWrite chunker.py — DocumentConverter at module level (NOT inside the function)`nRun the file once standalone to trigger Docling model download (~1.5GB, one-time)`nImplement extract_and_chunk() — Docling convert → export_to_markdown() → RecursiveCharacterTextSplitter`nTest on a real PDF: python -c "from api.chunker import extract_and_chunk; print(extract_and_chunk('test.pdf'))"`nVerify chunks are clean, section headers preserved, chunk_id starts at 0`n`nDay 4 — wire everything + end-to-end test`nComplete main.py: read file with await file.read(), check magic bytes (%PDF), MD5 check, extract_and_chunk(), init_job()`nAdd sequential for-loop placeholder (process_chunk_local stub that just writes dummy result to Redis)`nAdd _watch_and_aggregate stub — just polls until done, logs 'ready to aggregate'`nAdd GET /status, GET /result, GET /quota endpoints`nTest full flow with curl — upload PDF, get job_id, poll status, see done_chunks count up`nTest rate limit: upload 3 times, third should return 429`nTest MD5: upload same file twice, second should return cached result instantly`n`nDeliverable: curl POST /ingest returns job_id. GET /status shows progress. Rate limit and MD5 cache both work. No LLM output yet — placeholder results only.`n`n`nPhase 2  —  Beam Integration`nDays 5–9`n`nGoal`nBy end of day 9, chunks are dispatched to real Beam containers running in parallel. You can watch done_chunks increment in Redis as each worker finishes. The GPU endpoint (vllm_server.py) is live and returning text. The CPU workers (worker.py) are deployed and calling it. No aggregation yet.`n`nDay 5 — Beam account + CLI setup`nSign up at beam.cloud`npip install beam-client`nbeam login — follow the browser prompt`nAdd Beam credentials to .env: BEAM_CLIENT_ID, BEAM_CLIENT_SECRET`nRun: beam token create — save output as BEAM_AUTH_TOKEN in .env`nAdd all secrets to Beam: beam secret create REDIS_URL ..., beam secret create BEAM_AUTH_TOKEN ...`nAdd placeholder: beam secret create VLLM_ENDPOINT_URL placeholder`n`nDay 6 — vllm_server.py`nWrite beam/vllm_server.py — @endpoint with T4 GPU, keep_warm_seconds=60`nPin CUDA correctly in Image(): torch==2.3.0+cu118 with --index-url pytorch whl/cu118`nWrite the generate() function — global _llm loaded once per container`nWrite call_vllm() HTTP helper and clean_json() fence stripper`nDo NOT run locally — vLLM requires a real GPU`n`nDay 7 — deploy GPU endpoint + first cold start`nRun: beam deploy beam/vllm_server.py:generate --name qwen-vllm`nBeam prints the endpoint URL — copy it`nUpdate the secret: beam secret create VLLM_ENDPOINT_URL https://your-url.app.beam.cloud`nAdd VLLM_ENDPOINT_URL to local .env`nWarm up the endpoint — send a test curl request and WAIT (first call = 60-90s — model downloading)`nConfirm: curl returns {"text": "..."} — endpoint is working`n`nDay 8 — worker.py`nWrite beam/worker.py — @task_queue, CPU only (no GPU), memory=512`nImport call_vllm and clean_json from beam.vllm_server`nBuild the Qwen chat-format prompt inside process_chunk()`nParse JSON response with Pydantic ChunkAnalysis model`nWrite atomic Redis result with RPUSH + HINCRBY pipeline`nWrite fallback handler — never stall the job on one bad chunk`nWrite process_chunk_local() — identical logic, no Beam dispatch, for benchmark mode`n`nDay 9 — deploy worker + swap main.py loop`nRun: beam deploy beam/worker.py:process_chunk --name pdf-chunk-processor`nIn main.py: replace the sequential for-loop with process_chunk.put(job_id=job_id, chunk=chunk) per chunk`nUpload a PDF and watch Beam dashboard — N tasks should appear simultaneously`nPoll /status and verify done_chunks increments as workers finish`nCompare timing: note how fast parallel is vs the sequential stub from Phase 1`n`nDeliverable: Upload PDF → N Beam tasks fire simultaneously → Redis done_chunks counts up in real time. Each chunk has a real LLM summary from Qwen2.5-7B. GPU cost per run: ~$0.002.`n`n`nPhase 3  —  LLM Output`nDays 10–13`n`nGoal`nBy end of day 13, GET /result returns a complete structured JSON — abstract, top 10 key points, and 4-section documentation (intro, methods, findings, conclusion). MD5 cache is verified working. Rate limit is tested. The project is functionally complete.`n`nDay 10 — aggregator.py`nWrite beam/aggregator.py — import get_job, set_final_output, cache_result_by_md5, call_vllm, clean_json`nDefine FinalOutput Pydantic model at module level (not inside function)`nSort results by int(chunk_id) — explicit cast for JSON round-trip safety`nBuild digest string from sorted chunk summaries`nWrite the final Qwen prompt — request abstract + top_key_points[10] + documentation{intro, methods, findings, conclusion}`nParse response through FinalOutput Pydantic model`nWrite fallback: if parsing fails, use chunk summaries as key_points`nCall set_final_output() then cache_result_by_md5() at the end`n`nDay 11 — _watch_and_aggregate in main.py`nReplace the stub watcher with the real _watch_and_aggregate() async function`nUse asyncio.sleep(2) in the polling loop — NOT time.sleep (blocks the event loop)`nCall aggregate() via await loop.run_in_executor(_executor, aggregate, job_id) — non-blocking`nAdd 300s max_wait timeout — if exceeded, set status='error' in Redis`nCreate ThreadPoolExecutor at module level: _executor = ThreadPoolExecutor(max_workers=4)`nDo the same for _run_sequential — also use run_in_executor for blocking LLM calls`n`nDay 12 — full pipeline test on real papers`nDownload 3 real research papers (arxiv has free PDFs)`nUpload each one and wait for full completion`nInspect the JSON output — check abstract quality, key points relevance, documentation sections`nVerify done_chunks == total_chunks before result is available`nVerify failed_chunks count is reported correctly if any chunk fails`nCheck Redis keys after each run — confirm TTLs are set correctly`n`nDay 13 — MD5 + rate limit verification`nRe-upload the same paper from day 12 — confirm instant return with cached:true`nVerify the re-upload does NOT increment the rate limit counter`nUpload 2 different papers — confirm both succeed`nUpload a 3rd different paper — confirm 429 with resets_in_minutes in the response`nCall GET /quota — verify uploads_used and resets_in_minutes are accurate`nWait for TTL expiry (or manually delete Redis key) and confirm limit resets`n`nDeliverable: GET /result returns full structured JSON. Same PDF uploaded twice returns instantly. Third different PDF returns 429. Project is functionally complete — everything works.`n`n`nPhase 4  —  Polish & Deploy`nDays 14–21`n`nGoal`nBy end of day 21 the project has a live public URL, a Streamlit frontend anyone can use, a README with real benchmark numbers from your machine, and a demo GIF. This is what goes on your portfolio and GitHub profile.`n`nDay 14 — SSE /stream endpoint`nAdd GET /stream/{job_id} to main.py using StreamingResponse`nGenerator function: poll Redis every 0.5s, yield new chunk results as SSE events`nTrack seen chunk_ids in a set — only yield each chunk once`nBreak on status=='done' — yield final output event`nBreak on status=='error' — yield error event`nAdd 300s timeout — break and stop the generator`nTest with curl --no-buffer http://localhost:8000/stream/{job_id}`n`nDay 15 — Streamlit frontend`nWrite frontend/app.py`nFile uploader widget — accepts PDF only`nOn upload: POST to /ingest, store job_id in session state`nProgress bar: poll /status every second, update bar with done_chunks/total_chunks`nResults panel: show abstract, top key points as a list, documentation as expandable sections`nQuota display: show GET /quota result — uploads remaining + reset time`nRun: streamlit run frontend/app.py`n`nDay 16 — benchmark.py`nRun benchmark.py against a real 40-page research paper`nRecord both times: parallel and sequential`nCalculate speedup: sequential_time / parallel_time`nThis number goes in your README title — write it down`nRun 3 times and take the average — first run may be slower due to GPU warm-up`n`nDay 17 — production deployment`nUpstash Redis: go to upstash.com, create database, copy production REDIS_URL`nUpdate Beam secret: beam secret create REDIS_URL <production-upstash-url>`nDeploy FastAPI to Railway: push repo to GitHub, connect in Railway, set env vars`nSet all env vars in Railway: REDIS_URL, VLLM_ENDPOINT_URL, BEAM_CLIENT_ID, BEAM_CLIENT_SECRET, BEAM_AUTH_TOKEN`nTest the live URL end-to-end — upload a PDF through the production endpoint`n`nDays 18–19 — README + demo assets`nREADME headline: 'Processed a 40-page PDF in Xs (parallel) vs Ys (sequential) — Zx speedup'`nArchitecture section: draw the system diagram (use Excalidraw or screenshot the doc diagram)`nTech stack table: Docling, vLLM, Beam, Qwen2.5-7B, Redis, FastAPI`nSetup instructions: pip install, beam deploy order, env vars`nRecord demo GIF: use Gifox or ScreenToGif — show upload → progress bar → results appearing`nDeployment section: link to live URL`n`nDays 20–21 — buffer + final cleanup`nHandle edge cases: empty PDF, corrupted file, PDF with only images and no OCR`nAdd file size validation message to Streamlit UI`nClean up any debug print statements`nMake sure all secrets are in .env.example (without values)`nFinal GitHub push — clean commit history, good commit messages`nShare the repo URL`n`nDeliverable: Live URL. Streamlit demo. README with real benchmark numbers and architecture diagram. Demo GIF. Clean GitHub repo. Portfolio-ready.`n`n`nSummary — All 21 Days`nDay`nPhase`nTask`n1`n1`nRepo setup, venv, requirements.txt, FastAPI skeleton, local Redis`n2`n1`ntracker.py — all Redis helpers (jobs + rate limit + MD5) complete`n3`n1`nchunker.py — Docling at module level, extract_and_chunk() working`n4`n1`nWire main.py end-to-end, test with curl, rate limit + MD5 verified`n5`n2`nBeam account + CLI, beam login, all secrets created`n6`n2`nvllm_server.py — GPU endpoint, CUDA pinned, call_vllm() + clean_json()`n7`n2`nbeam deploy vllm_server — wait for cold start, verify endpoint returns text`n8`n2`nworker.py — CPU task queue, Qwen prompt, atomic Redis write, fallback`n9`n2`nbeam deploy worker — swap main.py loop to process_chunk.put() fan-out`n10`n3`naggregator.py — FinalOutput schema, digest, final prompt, MD5 cache`n11`n3`n_watch_and_aggregate — run_in_executor, asyncio.sleep, 300s timeout`n12`n3`nFull pipeline test on 3 real research papers`n13`n3`nMD5 dedup + rate limit verification — all scenarios tested`n14`n4`nSSE /stream endpoint — real-time chunk delivery to client`n15`n4`nStreamlit frontend — upload, progress bar, results, quota display`n16`n4`nbenchmark.py — run on real PDF, record headline speedup number`n17`n4`nProduction deploy — Upstash Redis + Railway for FastAPI`n18`n4`nREADME — headline, architecture diagram, tech stack, setup instructions`n19`n4`nDemo GIF recording + add to README`n20`n4`nEdge cases, error messages, debug cleanup`n21`n4`nFinal GitHub push — clean history, repo ready to share`n`n`nasync-pdf-pipeline  —  build it in order, test each phase before moving on.`n
